[project]
name = "www_starbug_com"
version = "2026.5.9"
description = "This is the source for the starbug.com web site"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask",
    "gunicorn",
]
